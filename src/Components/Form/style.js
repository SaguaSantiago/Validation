import { makeStyles } from "@material-ui/core"

export const useStyles = makeStyles({
  flexItem: {
    marginBottom: "40px",
    marginTop: "40px",
    width: "60%",
  },
  input: {
    marginBottom: "20px",
    width: "100%",
  },
  button: {
    marginBottom: "20px",
    alignItems: "center",
    marginTop: "20px",
  },
})
